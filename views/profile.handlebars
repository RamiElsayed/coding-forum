{{> memberNavbar}}
<main>
    <div class="jumbotron text-center">
        <h1 class="display-4">dashboard</h1>
        <hr class="my-4" />
        <p class="lead">let's start learning together</p>
    </div>
    <div class="d-flex flex-row justify-content-center flex-wrap">
    {{#each threads }}
      {{> threadCard}}
    {{/each}}
    </div>
</main>